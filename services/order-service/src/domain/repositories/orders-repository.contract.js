/**
 * Contrato que cualquier implementación de OrdersRepository debe cumplir.
 * En JavaScript esto es documentación + convención, no una garantía del compilador
 * (esto es justo el costo de no tener TypeScript que hablamos en la Unidad 4).
 *
 * save(orderData)          -> Order
 * findAll()                -> Order[]
 * findById(id)              -> Order | undefined
 * deleteById(id)            -> boolean
 * updateStatus(id, status)  -> Order | undefined
 */
module.exports = {};